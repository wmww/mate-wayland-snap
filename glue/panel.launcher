#!/bin/bash
set -e

# For a classic snap this needs to be something sane
export XDG_RUNTIME_DIR=/run/user/$(id -u)
echo "Starting MATE Panel on Wayland display ${WAYLAND_DISPLAY:-[nil]}"
exec $SNAP/usr/bin/mate-panel $@
